export function TodayRing() {
  // Home no longer exposes a standalone Rest Insights shortcut.
  // Rest Insights remains available from the ORB hold menu.
  return document.createDocumentFragment();
}
